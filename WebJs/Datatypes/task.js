// Read a text from the browser using prompy and display n0.0f.characters present in that text.
let str=prompt("Enter a String");
let str1=str.length
console.log(`Number of characters presemt is ${str1}`);